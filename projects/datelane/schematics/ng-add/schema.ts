/** Options accepted by the `ng add @datelane/core` schematic (mirrors schema.json). */
export interface Schema {
  /** Target project to receive the scheduler stylesheet; defaults to the workspace default. */
  project?: string;
}
